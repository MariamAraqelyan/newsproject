import { AllowForRolesDirective } from './allow-for-roles.directive';

describe('AllowForRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowForRolesDirective();
    expect(directive).toBeTruthy();
  });
});
